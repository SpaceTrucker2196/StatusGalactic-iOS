# 2026-06-30

## State on arrival

- Branch `main`, ahead of `3653c63` (v0.2.1 build 1 train opened)
- Two new widgets in progress, not yet committed:
  - `StatusGalacticWidget/SolarTerrestrialWidget.swift` (463 LOC) — small + medium SolarHam-style HF readout
  - `StatusGalacticWidget/TidesWidget.swift` (349 LOC) — small (next 2 events) + medium (curve + next 3 events)
- Both wired into `StatusGalacticWidgetBundle.swift`
- Both share `BriefWidgetProvider`, so a single brief fetch feeds the whole bundle
- All four `Info.plist` files bumped 0.2.0 → 0.2.1
- `StatusGalacticWidget/Info.plist` gained `UIAppFonts` (FiraCode family) — needed for the widget to render `.firaCodeFixed(...)` chrome instead of falling back to system font
- `ROADMAP.md` grew Phase E (M17–M22, all ✅) + Phase F (M23–M26) + Later (M27–M28); historical backend table preserved at the bottom
- No `memory/`, no daily notes
- `IDENTITY.md` + `USER.md` still template stubs

## Review notes (widgets)

- All field references resolve against `Brief.swift`: `space.solarFlux`, `space.kpIndex`, `space.observedAt`, `xRay.{currentClass,peakClass24h,rScale,observedAt}`, `proton.{fluxPfu,sScale}`, `aurora.{localProbabilityPct,globalMaxPct}`, `flareProbability.{mClassPct,xClassPct}`, `bandConditions[].{dayStatus,nightStatus}`, `solarOutlook[].aIndex`, `solarCycle[].sunspotNumber`, `wwvBulletin.aIndex`, `tides.{stationId,stationName,distanceKm,events}`, `TideEvent.{time,heightFt,kind}`, `Brief.timezone`
- Widget target sources include `GalacticPalette.swift`, `Typography.swift`, fonts dir — `.firaCodeFixed(...)`, `.neonGlow(...)`, `GalacticPalette.cosmicSky/phosphorGreen/etc` all available
- `BriefWidgetProvider` reused unchanged — single Brief fetch every 30 min covers all three widgets (Brief/Solar/Tides)
- Not yet verified: an Xcode build. Worth running before commit since two large new files were added without compile feedback

## Open follow-ups

- ~~Build the widget extension to confirm the new files compile (only verified by inspection)~~ ✅ build green via `xcodebuild -scheme StatusGalactic`
- M24 still has two pending deliverables per the new ROADMAP: manual home-screen widget shot + privacy nutrition labels in App Store Connect
- M25 / M26 (real-hardware verification, TestFlight push) still ⏳

## Android side — `~/projects/StatusGalactic-Android`

Picked up the port mid-stride. Before this session: A1, A2, A6 ✅; A3, A4, A5, A7 🚧. Working tree already had Marine/Launches/Alerts clients, `Planets.kt`, `SiderealClock.kt`, `SunStrip.kt`, plus model + BriefBuilder + BriefScreen edits — all sitting uncommitted from a prior session.

Found the build was failing only because `BriefScreen.kt` referenced three undefined composables (`WeatherAlertsCard`, `PlanetsCard`, `LaunchesCard`). Wrote them following the existing `GalacticCard` / `PhosphorHeader` pattern with severity-tiered palette colors (Extreme→Severe, Severe→Storm, etc.), then `./gradlew assembleDebug` → BUILD SUCCESSFUL.

Committed as `ec1047e` — "A3-A5: Marine + Launches + Alerts clients, Planets ephemeris, brief cards." Code-only; `MIGRATION_PLAN.md` + `README.md` status tables not yet updated to reflect the new ✅/🚧 state.

### Android progress this morning

Two more commits landed:

- `ec1047e` — A3-A5: Marine + Launches + Alerts clients, Planets ephemeris, brief cards.
- `d9317d8` — A7-A8: `SettingsStore` (DataStore: callsign, aprs.fi key, marine zone, aurora %, storm level) + `SettingsViewModel` + sectioned `SettingsScreen` with sliders. `BriefUiState.Loaded` now carries `fetchedAtMs`, `BriefBody` wraps in `PullToRefreshBox`, `HeaderCard` shows `relativeTime(...)`. Marine zone threads from settings → `BriefBuilder.buildBrief(marineZoneId = ...)`. `README.md` + `MIGRATION_PLAN.md` status tables synced.

Build: `./gradlew assembleDebug` green after each commit.

### Open items on the Android side

- **A7 polish:** marine zone is a text field today. Full `MarineZoneCatalog` picker port (~119 lines on iOS) is the remaining A7 gap.
- **A9 polish:** per-section error footers (Brief errors today render as a single bottom card row).
- **A4 long tail:** aprs.fi locate, OVATION, DONKI, GOES X-ray, USGS earthquakes, NEO, Mars, APOD, RepeaterBook, POTA, SOTA, DX cluster, WWV, ionosondes, river gauges, magnetic declination.
- ~~**A10:** wire `SunStrip` into the Sun card~~ ✅ landed in `4574a92` — strip sits between the SUN header and the UTC sunrise/sunset rows. `Clock.System.now()` at composition (wall clock, not fetch time). Docs synced in `a6d3947`.
- **A11:** callsign live position (aprs.fi locate + map).

### Later same day — A7 closed + WORKBOARD.md added

Two commits on the Android side:

- `fd70299` — A7: MarineZonePickerSheet + WORKBOARD milestone view. Verbatim port of `MarineZoneCatalog.swift` (~50 curated NWS zones, `orderedRegions` list, `nameFor(code:)` helper). New `MarineZonePickerSheet` is a Material 3 `ModalBottomSheet` with a search field, region-grouped rows, "None / inland" at top, "Custom code" block at bottom. `SettingsScreen` now shows a two-line `MarineZoneRow` (code on top, friendly name below) that opens the sheet. Also added `WORKBOARD.md` at Android repo root — glanceable phase-A→phase-E milestone board with landing commits.
- `e4e02e4` — WORKBOARD: pin `fd70299` as A7 landing commit.

Build: `./gradlew assembleDebug` green (needed to prime `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home` first — user's shell doesn't set it).

Phase A remaining after this: A3/A4/A5 long-tail data sources, A9 per-section error footers. Everything past A10 unstarted; see `WORKBOARD.md` for phase B–E queue.

### Evening — A9 closed + two more A4 clients

- `0685039` — A9 + A4/A5: per-section error footers, USGS EarthquakeClient + EarthquakesCard. Each card now takes an optional error string and renders a `SectionErrorFooter` (⚠ + message) inline. Sections that fail with no data render an `EmptySectionCard` shell instead of vanishing. Bottom-of-list `SourceErrorRow` kept only as a catch-all for keys that don't map to a known section (e.g. `crewed` will fall through until the card is added). USGS EarthquakeClient mirrors the iOS impl — parallel fetch of `significant_week.geojson` + local M2.5+ within 500 km, deduped by event id, haversine-ranked. Card is magnitude-tiered (Calm→Severe), shows depth, distance-from-viewer, "significant" badge, relative age.
- `daa8d37` — A4/A5: ISSClient (wheretheiss.at, auth-free) + CrewedObjectsCard. `CrewedSpacecraftCatalog` scaffolded (ISS-only today, same catalog approach as iOS). N2YO visual-pass predictions from the iOS client deliberately skipped — API-key gated, and Android settings don't wire one yet.

A9 now ✅ in all docs. A4 at 7 of ~20 clients; A5 has 10 cards.

Next queue: GOES X-ray flux (needs `XRayState` model + sparkline in SpaceCard), NEO/APOD/DONKI (all api.nasa.gov, need settings to hold an API key; wire that first), OVATION aurora oval (image-based, needs disk-backed image cache — bigger).
